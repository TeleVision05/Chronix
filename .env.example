# Copy to `.env` (git-ignored) and fill in. Expo reads EXPO_PUBLIC_* at build/start time.
# Google Maps Platform key with Places API + Geocoding API enabled, restricted to this
# app's Android package (com.therune.Chronix) / iOS bundle id.
EXPO_PUBLIC_GOOGLE_MAPS_KEY=
